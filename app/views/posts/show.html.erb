<h1><%= @post.title %></h1>
<p><%= simple_format(@post.content) %></p>

<p>
  <%= link_to "수정", edit_post_path(@post) %>
  <%= button_to "삭제", @post, method: :delete, data: { turbo_confirm: "정말 삭제할까요?" } %>
</p>

<p><%= link_to "목록으로", posts_path %></p>
